# Sample PID Documents

Place your two P&ID PDF revisions here for demo and testing.

Expected naming convention:
- `pid_rev_A.pdf` — original revision
- `pid_rev_B.pdf` — revised version

These files are gitignored (large binary). Do not commit them.

**Provenance:** Document pairs should include a note about source (e.g., "publicly available example P&ID from [source]").
